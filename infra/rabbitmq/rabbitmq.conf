# Load AI/ML user and queue permissions on startup.
load_definitions = /etc/rabbitmq/definitions.json

# Docker image disables guest loopback restriction; keep guest for internal services only.
loopback_users.guest = false
